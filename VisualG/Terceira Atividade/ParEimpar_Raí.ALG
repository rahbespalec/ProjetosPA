Algoritmo "ParImpar"
// Autor(a)    : Raíssa Bespalec

var
num: inteiro

inicio
//Entrada de Dados

escreval("Digite o número que deseja saber se é ímpar ou par:")
leia(num)

se((num mod 2)=0)entao

   escreval("Esse número é um número par.")

senao

     escreval("Esse número é um número impar")

fimse
fimalgoritmo